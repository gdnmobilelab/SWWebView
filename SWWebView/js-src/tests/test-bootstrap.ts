export function describeIfApp(desc, target) {
    if ((window as any).NOT_APP === true) {
        xdescribe(desc, target);
    } else {
        describe(desc, target);
    }
}

import "../src/service-worker-container";

// This is generated by the rollup plugin
import "all-tests";
