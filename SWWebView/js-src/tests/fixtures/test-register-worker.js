// self.addEventListener("install", e => {
//     e.waitUntil(
//         new Promise((fulfill, reject) => {
//             // small delay so we can check different install states
//             setTimeout(fulfill, 1000);
//         })
//     );
// });
console.info("Registered");
